---
awx_volumes: >
  {{
    [project_data_dir + ':/var/lib/awx/projects:rw'] if project_data_dir is defined else []
    + [ca_trust_dir + ':/etc/pki/ca-trust:ro'] if ca_trust_dir is defined else []
    + custom_volumes if custom_volumes is defined else []
  }}
